import { WindowSideBar, type WindowSlot } from "../../components";

export const ProjectExplorerWindowSideBar: WindowSlot = ({ active }) => {
  return <WindowSideBar active={active}></WindowSideBar>;
};
